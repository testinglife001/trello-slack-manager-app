import React from 'react'

const CanvasNodes = () => {
  return (
    
    <div>CanvasNodes</div>
  )
  
}

export default CanvasNodes

/*
Usage inside Canvas
const visibleNodes = getVisibleNodes(camera, quadTree);

fabricCanvas.clear();

visibleNodes.forEach(node => renderNode(node));
*/

